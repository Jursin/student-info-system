# 项目要求
## 项目概述
- 网站名称： 学生信息管理系统
- 核心目标： 解决传统问卷收集学生信息方式存在的弊端（数据孤立、重复填写、信息变更不便、学生无法核验），构建一个基于数据库的、支持动态数据模型和细粒度权限管理的Web系统，实现信息的“一次录入，多方复用”，提升数据管理效率与安全性。
- 用户预期： 前期总数据量约300条，但系统架构需具备良好的可扩展性，以应对未来数据量增长和功能扩展。

## 功能详述

### 用户角色与权限

系统需严格区分以下角色及其权限：

* 学生：
   * 查看本人的所有信息（包括所有表中的本人数据）。
   * 编辑本人信息，但"学号"、"姓名"、"班级"固定，只由管理员导入。
   * 修改自己的密码。
* 班委：
   * 拥有学生的全部权限。
   * 额外可查看本班级（
"班级"字段值与班委本人相同）所有学生的信息（所有表），但无编辑权限。
* 管理员：
   * 拥有班委的全部权限。
   * 数据管理：可对所有表的数据进行增、删、改、查。
   * 表管理：可创建新的包括某些人员的表，创建时需自动从“基本信息”表导入"学号"、"姓名"、"班级"三列作为基础字段。可编辑/删除由自己创建的表。
   * 字段管理：在表内，可动态新增字段，并能为字段设置以下值类型/限制：
      * "文本" (默认，无限制)
      * "纯数字" (可选项：限制位数)
      * "纯汉字" (可选项：限制字数)
      * "日期" (提供日期选择器)
      * "单选" (从给定值列表中选择一个)
   * 操作日志：可查看所有用户的操作日志。
   * 数据导入/导出：支持Excel格式的批量导入与导出。
* 超级管理员：
   * 拥有管理员的全部权限。
   * 额外权限：可清空任何表的所有数据（危险操作需二次确认）。可删除任何表（无论创建者是谁）。可管理所有用户账号和角色。
   * 编辑任意角色权限。

### 核心功能模块

* 登录模块：
   * 方式：学号 + 密码。
   * 密码策略：至少10位，必须包含大小写英文字母和数字。
   * 安全策略：密码错误10次，账号锁定5分钟。
* 页面：
   * 主页默认为仪表盘，显示可查看的信息的图表
      * 学生：侧边栏信息表展开为所有包含自己信息的表，点击每个表后以表单形式显示信息
      * 非学生：点击每个表后以列表形式显示所有人员信息
* 信息管理模块：
   * 列表视图：以可排序、可筛选的表格形式展示表数据。
   * 表单操作：提供清晰易用的表单，用于新增、编辑单条数据。
   * 批量操作：支持数据的批量删除、批量导出。
* 表与字段管理模块 (管理员以上)：
   * 可视化界面，支持创建表、定义字段（名称、值类型、限制条件）。
* 个人中心：
   * 学生和班委查看/编辑本人信息、修改密码的入口、设置二步验证。
* 操作日志模块：
   * 按时间、操作者、操作类型（增、删、改）、操作表名等进行查询和筛选。
   * 自动分割：系统需定期（如按季度或按年）或当日志文件达到一定大小时，自动创建新的日志文件/分区，便于管理和查阅。
* 系统管理模块 (超级管理员)：
   * 用户账号管理、角色分配、表删除与清空等高级功能。

## 非功能需求

* 安全性：
   1. 传输安全：全站使用HTTPS。
   2. 密码存储：使用强哈希算法（如bcrypt）加盐存储密码。
   3. 权限校验：后端对每一个数据请求都必须进行角色与数据级别的权限校验（如班委查询时，SQL必须附加
"班级"条件）。
   4. 输入验证：前后端均需对输入进行严格验证和过滤，防止SQL注入和XSS攻击。
* 可维护性与扩展性：
   1. 数据模型：采用“实体-属性-值”(EAV)模式或类似动态模型存储用户自定义字段，或使用PostgreSQL的JSONB字段，以支持灵活的表和字段定义。
"学号"为全局唯一主键。
   2. SSO预留：用户认证模块设计需考虑解耦，为未来接入学校统一身份认证（SSO）预留接口。
* 易用性：
   1. 界面简洁直观，符合管理后台操作习惯。
   2. Excel导入模板可下载，导出数据格式规整。

## 技术栈

* 前端：
   * 框架：Nuxt 4
* 后端：
   * Node.js + Fastify​ + Prisma​ (ORM)
* 数据库：
   * PostgreSQL 18。
* 部署：
   * 初期：可部署在 Vercel (前端 + 连接 Neon 数据库)、Railway/Render (后端+DB) 等云服务平台，快速上线。
   * 后期：迁移至阿里云/腾讯云等云服务器，使用Docker容器化部署，配合Nginx反向代理。
